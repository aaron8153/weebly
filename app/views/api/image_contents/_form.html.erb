<%= form_for(@image_content) do |f| %>
  <% if @image_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_content.errors.count, "error") %> prohibited this image_content from being saved:</h2>

      <ul>
      <% @image_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content_id %><br>
    <%= f.number_field :content_id %>
  </div>
  <div class="field">
    <%= f.label :uri %><br>
    <%= f.text_field :uri %>
  </div>
  <div class="field">
    <%= f.label :target %><br>
    <%= f.text_field :target %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
