<%= form_for(@text_content) do |f| %>
  <% if @text_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_content.errors.count, "error") %> prohibited this text_content from being saved:</h2>

      <ul>
      <% @text_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :element_id %><br>
    <%= f.number_field :element_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
