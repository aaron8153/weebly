<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @page.title %>

  <strong>Elements:</strong>
  <% @page.elements.each do |e| %>
      <span>Element Id: <%= e.id %></span>
      <span>Content type: <%= e.content_type %></span>
  <% end %>
</p>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
